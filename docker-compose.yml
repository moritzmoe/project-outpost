version: '3.1'

services:

  adminer:
    image: adminer
    restart: always
    ports:
      - 9000:8080

  db:
    image: postgres
    restart: always
    environment:
        POSTGRES_PASSWORD: my_pass
    ports:
        - 5432:5432